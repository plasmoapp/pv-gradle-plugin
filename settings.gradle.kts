pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        mavenCentral()
        google()
        maven("https://maven.minecraftforge.net")
    }
}

rootProject.name = "pv-gradle-plugin"
